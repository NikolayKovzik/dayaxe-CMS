enum RoutesList {
  DEFAULT = "/",
  CMS_HISTORY = "/history",
  CONFIGURATION = "/configuration",
  TEXTS = "/texts",
  ADMNISTRATOR_PROFILE = "/adminprofile",
  NOT_FOUND = "*",
}

export default RoutesList;